---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: torch
  name: torch
spec:
  replicas: 1
  selector:
    matchLabels:
      app: torch
  template:
    metadata:
      labels:
        app: torch
    spec:
      serviceAccountName: torch
      containers:
      - name: torch
        image: ghcr.io/jrmanes/torch:v0.0.1
        command:
          - /bin/sh
          - -c
        args:
          - ./torch --config-file /config/config.yaml
        env:
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: config
          mountPath: "/config"
          readOnly: true
        resources:
          requests:
            memory: "50Mi"
            cpu: "50m"
          limits:
            memory: "100Mi"
            cpu: "100m"
      volumes:
      - name: config
        configMap:
          name: config
          items:
          - key: config.yaml
            path: config.yaml
